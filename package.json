{
  "name": "apartment",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"No tests configured\"",
    "lint": "echo \"No linting configured\"",
    "build": "pnpm --filter frontend-main build",
    "dev": "pnpm --parallel -r dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.33.0",
  "prisma": {
    "schema": "packages/prisma/schema.prisma",
    "seed": "node packages/prisma/seed.js"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "bcrypt",
      "@prisma/engines",
      "prisma",
      "@prisma/client",
      "esbuild"
    ]
  }
}
